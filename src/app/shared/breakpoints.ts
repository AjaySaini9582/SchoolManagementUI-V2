/** Matches Tailwind's default `md` breakpoint (768px) so Material
 * `BreakpointObserver`-driven layout switches (sidenav mode, table layout)
 * line up with any Tailwind responsive utility classes used alongside them. */
export const BELOW_MD_BREAKPOINT = '(max-width: 767.98px)';
